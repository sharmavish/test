name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Test
      if: contains(github.ref, 'development')
      run: |
       export TEST="HELLLOOOOOOOOOOOOOOOOOOOOOOO"
       echo $TEST && env | grep -i test
